apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hf-proxy-app.fullname" . }}-test-connection"
  labels:
    {{- include "hf-proxy-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hf-proxy-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
